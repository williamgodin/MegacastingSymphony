{# templates/login/index.html.twig #}
{% extends 'base.html.twig' %}

{# ... #}

{% block body %}
    {% if error %}
        <div class="d-flex justify-content-center alert alert-danger">L'utilisateur "<b>{{last_username}}</b>" n'existe pas </div>
    {% endif %}
    <div class="container justify-content-center d-flex pt-4 ">
    <form action="{{ path('login') }}" method="post">
        <label for="username">Login:</label>
        <input type="text" id="username" name="_username" value="{{ last_username }}"/>

        <label for="password">Password:</label>
        <input type="password" id="password" name="_password"/>

        {# If you want to control the URL the user is redirected to on success
        <input type="hidden" name="_target_path" value="/account"/> #}
        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
        <button type="submit">login</button>

        <p class="d-flex justify-content-center text"><a href="{{ path('app_register') }}">Si vous n'êtes pas encore inscrit cliquez ICI</a></p>
    </form>
    </div>
{% endblock %}
