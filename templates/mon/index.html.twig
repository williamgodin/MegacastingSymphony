{% extends 'base.html.twig' %}

{% block title %}Megacasting{% endblock %}

{% block body %}
<style>
    .example-wrapper {
        margin: 1em auto;
        max-width: 800px;
        width: 95%;
        font: 18px/1.5 sans-serif;
    }

    .example-wrapper code {
        background: #F5F5F5;
        padding: 2px 6px;
    }
</style>
<div class="container">
    <div class="row"></div>
        <div class="col-md-12 d-flex flex-wrap justify-content-around">
            {% for casting in castings %}
            {{ include('layout/littleCasting.html.twig', {'casting': casting}) }}
            {% endfor %}
            {% if castings == null  %}
                <p >Pas de correspondance à votre recherche </p>
            {% endif %}

        </div>
        {% endblock %}

